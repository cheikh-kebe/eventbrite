<!DOCTYPE html>
<html>
  <div class="row align-middle shadow p-3 mb-5 justify-content-center">
    <div class="card" style="width: 30rem;">
      <div class="card-body">
        <h5 class="card-title"><%= @user.first_name %></h5>
        <h6 class="card-subtitle mb-2 text-muted"><%= @user.last_name %></h6>
        <p class="card-text"><%= @user.description %></p>
        <p class="card-text"><%= @user.email %></p>
        <p class="card-text"><%= @user.encrypted_password %></p>
      </div>
    </div>
  </div>
  <button type="button" class="btn btn-warning"><%= link_to 'Edit', edit_user_path(@user) %></button> |
  <button type="button" class="btn btn-light"><%= link_to 'Back', users_path %></button>
</html>
